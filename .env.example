# Replit injects DATABASE_URL automatically for the managed PostgreSQL database.
# Keep secrets in Replit Secrets; do not commit real values here.
DATABASE_URL=
SESSION_SECRET=
SERPAPI_API_KEY=
NODE_ENV=development